# Data Contract: Publisher

## القرار المعماري
نفس منطق Author تماماً — Taxonomy مخصصة `mkt_publisher`.

## الحقول (Term Meta)
```
name, slug           (native)
description          (native term description)
logo                 (Term Meta: mkt_publisher_logo)
is_active             (Term Meta: mkt_publisher_is_active)
```
